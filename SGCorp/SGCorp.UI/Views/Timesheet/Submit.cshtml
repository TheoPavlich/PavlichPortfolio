@using System.Web.Mvc.Html
@model SGCorp.UI.Models.EmployeeTimesheetVM
@{
    ViewBag.Title = "Submit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Timesheets: Select Employee</h2>

<div class="row">
    <div class="clo=lg-6">
        @using (Html.BeginForm("Submit", "Timesheet", FormMethod.Post))
        {
            <div class="form-group">
                <label>Select Employee</label>
                @Html.DropDownListFor(m => m.SelectEmployeeId, Model.Employees, new {@class = "form-contorl"})
            </div>
            <button class ="btn btn-primary">Select this Employee</button>
        }
    </div>
</div>

